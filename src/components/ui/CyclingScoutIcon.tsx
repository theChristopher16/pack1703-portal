import React, { useState, useEffect } from "react"; const CyclingScoutIcon = ({ className = "", size = 16, interval = 3000, monochrome = false }) => { const [currentIconIndex, setCurrentIconIndex] = useState(0); const icons = ["*", "���", "���", "���", "���", "���"]; useEffect(() => { const timer = setInterval(() => { setCurrentIconIndex((prev) => (prev + 1) % icons.length); }, interval); return () => clearInterval(timer); }, [interval]); return <span className={className} style={{ fontSize: size + "px" }}>{icons[currentIconIndex]}</span>; }; export default CyclingScoutIcon;
